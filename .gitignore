# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Build
dist/
dist-ssr/
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs
*.njsproj
*.sln
*.sw?

# Old versions
archive/

# Python
__pycache__/
*.py[cod]
.venv/
.coverage

# Build intermediates
.build/

# Data (regenerated by pipeline: crawl → llm_translate → build)
data/*
!data/overrides.yaml
!data/statuses.yaml
# Refactor: cfg-pipeline manual files + version archive are committed.
!data/cfg_aliases.yaml
!data/source_corrections.yaml
!data/key_normalization.yaml
!data/cfg/
data/cfg/cfg_current.json
!data/.cfg_history/

# cache, settings, ai-related
.cache/
.claude/
.pytest_cache/
ai-doc/
.playwright-mcp/
.grok/
_freeze/

# Supabase CLI local state (per-machine: project ref, pooler URL, version pins)
supabase/.temp/
supabase/.branches/

# Secrets — catch all env variants (.env, .env.dev, .env.local, …) but
# keep .env.example checked in as documentation of expected vars.
.env
.env.*
!.env.example
